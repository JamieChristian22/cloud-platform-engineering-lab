apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
namePrefix: prod-
patchesStrategicMerge:
  - patch-replicas.yaml
configMapGenerator:
  - name: enterprise-web-config
    behavior: merge
    literals:
      - ENVIRONMENT=prod
      - LOG_LEVEL=info
