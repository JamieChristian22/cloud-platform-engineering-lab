# Implementation Guide

Run from `environments/dev`:

```bash
terraform init
terraform validate
terraform plan
terraform apply
```

Then validate resources in AWS Console and run `terraform destroy` when finished.
