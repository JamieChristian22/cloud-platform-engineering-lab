apiVersion: v1
kind: Secret
metadata:
  name: observability-secret-template
  namespace: observability
type: Opaque
stringData:
  # Replace values locally or through your secret manager.
  # Do not commit real credentials.
  grafana-admin-user: "CHANGE_ME"
  grafana-admin-password: "CHANGE_ME"
